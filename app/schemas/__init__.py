"""Pydantic v2 schema package. All request/response bodies live here.

Convention: on-the-wire keys are `camelCase` (matching the frontend's TypeScript
types), while Python attributes stay `snake_case` thanks to `CamelBaseModel`."""
